# Core dependencies
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database
sqlalchemy>=2.0.0

# Telegram (for later phases)
telethon>=1.33.0
aiogram>=3.0.0

# GitHub (for later phases)
# Git operations will use subprocess, no GitPython needed

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Scheduler (for later phases)
apscheduler>=3.10.0

# Utilities
python-dateutil>=2.8.0
